import library "GreetingsBotMoodle/GreetingsBot-Lib.xmi"
import library "CoreLibrary"
import library "CoreLibrary"
import platform "MoodlePlatform"

use provider MoodlePlatform.MoodleIntentProvider

on intent Default_Fallback_Intent do
	action MoodlePlatform.PostMessage(restApiEndpoint : config(xatkit.moodle.restApiEndpoint) + "?wstoken=" + config(xatkit.moodle.accessToken), toUserId : "1", message : "PRUEBA_BOT")
